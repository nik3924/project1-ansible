---
# This playbook deploys the whole application stack in this site.


- name: configure and deploy the webservers and application code
  hosts: webservers
  remote_user: jenkins
  become: yes
  
 - name: Change permisssions of the instannce-1.pem file
   file: path=/var/lib/jenkins/workspace/project1-ansible/instannce-1.pem  owner=jenkins group=jenkins mode=400 


  roles:
    - web

